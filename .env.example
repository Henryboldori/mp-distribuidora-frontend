# Copie para ".env" e ajuste.
# Em desenvolvimento local, deixe assim:
VITE_API_URL=http://localhost:3001

# Em producao (depois do deploy no Render), troque pela URL real do backend, por exemplo:
# VITE_API_URL=https://mp-distribuidora-backend.onrender.com
